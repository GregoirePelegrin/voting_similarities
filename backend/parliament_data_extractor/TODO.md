- [x] check clusterization ok
- [x] display clusters
- [x] auto search for minimal MCA n_components 
- [x] must get a list of categories for the vote
   - get a definitive list of categories
- [x] must store data into a Postgres
   - members OK
   - votes (index, title, categories)
   - bulletins (vote_index, member_index, vote) => need to get the IDs from the already populated tables
   - do a continuation mechanism (to be able to split the injection)
- [x] plug real data
- [x] find MCA best n_components
   - use `plot_cumulative_inertia` in `compute_dimension_reduction`
- [x] run the algorithm on real data
- [ ] filter on vote categories
- [ ] k-clustering
- [ ] compute the most different groups