It seems some items weren't properly implemented:
- people detail view - answer bar:
    shows only 1 segment instead of 100
- people detail view - category alignment:
    the bars have a width set to `${width}%` with `width = abs(alignment) * 50`
    I don't understand this scale?
    To allow for arbitrary long category names, we should divide the card horizontally
    into a vertical "category" box, which displays a list of the names,
    and another vertical "alignment" box, taking the rest of the space in the parent.
    In this "alignment" box, the alignment bars range is -1 to +1, fixed.